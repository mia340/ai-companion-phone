# 新对话请先阅读 · V0.4.3.6

请先完整阅读 `AI续开发交接说明_V0.4.3.6.md`，再检查 V0.4.3.6 项目 ZIP。

当前基线：应用 V0.4.3.6 / IndexedDB V10 / backup V9。

本版核心原则：**社区 JSON 自带 UI / 状态栏 / 固定输出格式时，社区格式优先，小手机自己的动作/对白协议自动让位。**

关键改动：

- 删除“动作与对白排版”的手动选择；普通角色只按场景自动 remote 分开 / together 合并。
- 检测到社区 UI 时不注入 `scene_action / companion_packet`，也不二次拆分/合并作者格式。
- assistant-output Regex 在 UI 模式直接处理模型 raw output，避免 XML/状态标签先被默认解析器吃掉。
- Full HTML Regex 输出会拆成 style + body fragment 再进入 Shadow DOM。
- Prompt Debug 显示“社区 UI 输出接管”。
- 墨清尘真实 JSON 识别为 `html-contract`；“离婚的诱惑”真实 JSON 识别为 `regex-html`。

V0.4.3.5 的 User Persona 识别和初始状态修复继续保留。

下一主线：V0.4.3.7 WorldBook Engine V2 + Preset Prompt Manager + Regex 编辑器 + Theme Runtime V1。
