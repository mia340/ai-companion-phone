# V0.5.0-alpha.3.5 部署与更新

## 本版做了什么

### 1. App 图标系统

- 首页 12 个 App 使用统一 SVG 图标，不再使用 Emoji 作为主图标。
- Dock 同步使用同一套图标。
- 首页点击右上角「编辑」后，可以点任意 App 上传自己的图片替换图标。
- 自定义图标按当前世界保存到 IndexedDB V16。
- 图片在浏览器本地裁成 512×512 并压缩为 WebP，不上传服务器。
- Backup 升级到 V11，会一起保存 App 图标。

### 2. 多聊天记忆

一个角色有多个聊天时，**其他 App 不再选择某一个聊天作为记忆源**。

- `character`：角色共享记忆，跨聊天可读。
- `conversation`：当前剧情线记忆，只属于当前聊天。
- `fact / promise / relationship` 默认属于角色共享；`shared` 共同经历默认留在当前剧情线。
- `subjective / story` 默认属于当前聊天。
- 记忆管理可以手动在两种 Scope 之间切换。
- Chat Prompt 读取「当前聊天 + 角色共享记忆」。
- 朋友圈发帖和回评读取角色共享记忆。
- rewind / branch / 重开 / 删除聊天不会误删角色共享记忆。

## Windows 更新

建议从当前 git 工作区直接更新源码，不要覆盖 `.git`、`node_modules`、`dist`。

```powershell
cd "D:\ai"

robocopy `
  ".\ai-companion-phone-v0.5.0-alpha.3.5" `
  ".\ai-companion-phone-git-clean" `
  /E `
  /XD .git node_modules dist `
  /XF "*.tsbuildinfo" `
  /R:2 `
  /W:1

cd "D:\ai\ai-companion-phone-git-clean"

Select-String '"version"' package.json
npm run cleanup
npm test
npm run build
npm run dev
```

版本应显示：

```text
0.5.0-alpha.3.5
```

## 本版验收重点

1. 首页点击「编辑」→ 修改聊天 / 朋友圈 / 设置等 App 图标 → 刷新页面，图标仍然存在。
2. 上传一张很大的图片，确认会被裁切/压缩，而不是把原图直接塞进 IndexedDB。
3. 创建同一角色的两个聊天：聊天 A 产生一条事实记忆；聊天 B 应能命中该共享事实，但不应该看到聊天 A 的剧情 `story` 记忆。
4. 在记忆管理中把一条 `story` 记忆点「跨聊天共享」，切换另一个聊天后应能命中它；再点「移出共享」，它应重新隔离。
5. 删除 / branch / rewind / 自由开局后，角色共享记忆仍然存在。
6. 朋友圈由角色发布动态时，可以自然参考角色共享记忆，但不应直接复述整条记忆列表。

## 注意

当前源码仍属于 `0.5.0-alpha` 开发线。先完成 Windows `npm test` + `npm run build`，再部署 GitHub Pages。
