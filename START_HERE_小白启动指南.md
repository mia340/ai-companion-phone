# 从这里开始

## 1. 安装 Node.js

安装 Node.js 20 或更高版本。

## 2. 用 VS Code 打开项目文件夹

打开完整的项目文件夹，不要只打开某一个文件。终端路径中应能看到 `package.json`。

## 3. 第一次运行

在 VS Code 中点击“终端 → 新建终端”，依次执行：

```bash
npm install
npm run dev
```

终端会显示类似 `http://localhost:5173` 的地址，按住 Ctrl 点击即可打开。

## 4. 修改后检查

准备提交 GitHub 前运行：

```bash
npm run build
```

看到 `built in` 和 PWA 文件生成提示，说明构建通过。

## 5. 使用图片理解

进入：

```text
设置 → API 与模型 → 图片理解
```

先配置并保存支持视觉输入的模型，然后点击“测试图片理解”。聊天中点击输入框左侧的“＋”，选择图片、添加附言并发送。

图片理解取决于具体模型与接口是否接受 OpenAI 兼容的多模态 `image_url` 格式。不支持时，应用会自动使用不猜测图片细节的自然回应。

## 6. 在手机上测试

电脑与手机连接同一个 Wi-Fi，然后运行：

```bash
npm run dev -- --host 0.0.0.0
```

用手机浏览器打开终端显示的局域网地址。正式部署到 HTTPS 后，再测试“添加到主屏幕”。

## 当前版本

V0.3.2：多模态图片理解、图片附言与发送预览、能力检测、失败重试、停止等待，以及可选图片备份。

V0.3.3：流式回复、停止后保留部分内容、异常消息恢复，以及消息气泡和输入区组件拆分。

V0.4.0：角色卡 V2、用户 Persona、世界书、沉浸 Prompt 编排、候选回复、聊天分支与 OOC 导演指令。

V0.4.1：连续短消息、表情和语音样式互动、角色状态协议、相关记忆命中、Prompt 调试器，以及 SillyTavern JSON 角色卡导入导出。

V0.4.2：六层长期记忆、记忆冲突处理、持续状态 V2、主动消息频率与安静时段、小手机停顿/撤回/回应动作，以及 Prompt 预算和自然度调试。
V0.4.2.1：场景距离双模式消息；在身边时动作以括号进入同一剧情气泡，远程时角色动作独立显示、对白真正拆成多条手机消息，并修复记忆分类、重复、冲突与“下周三”日期。

记忆管理入口：

```text
聊天页 → 右上角 ··· → 记忆 → 打开完整记忆管理
```


V0.4.2.3：修复双模式版本严格 TypeScript 构建中的 `displayedConversationState` 未使用错误；数据结构不变。

V0.4.2.4：Persona 支持 JSON/TXT/MD 导入、导入预览、同名处理、V2 JSON 导出和 Tavo/酒馆资源识别；角色卡可显式转换为用户人设，世界书/预设/正则会阻止误导入。
