# alpha.5.1.5 更新与部署

解压到一个全新的目录，在目录中运行 `npm ci`、`npm run verify`。测试和构建都通过后，才按既有 GitHub Pages 流程部署；请勿清空站点数据。

验收：手机桌面和 Dock 中合并的社交 App 显示「知间」；打开主聊天页标题为「知间」；「我」页、资料编辑和钱包说明不再出现旧的「伴信」。四个内部标签和历史数据照常工作。

本次只改显示名称。历史数据库图标键 `banxin` 与路由 `/companion` 刻意保持不变，保证已有自定义图标和用户内容继续可用。
