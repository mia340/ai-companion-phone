# V0.5.0-alpha.5.1.12 Release Manifest

- Version: `0.5.0-alpha.5.1.12`
- IndexedDB: V18
- Backup: V12
- Test definitions: 41 files / 333 declarations
- Launcher Grid V6: App/Widget unified placement, swap, resize and cross-page movement.
- Widget defaults: companion/world 2×1, music 4×1; photo/calendar added.
- Editing UI: no upper-right Done block; background tap exits edit mode.
- Runtime: Character Card import report, Context Inspector, WorldBook per-entry activation decision trace, AgentAction capability boundary.
- Backup: Zod envelope preflight plus duplicate-ID / cross-table reference-integrity validation before destructive restore.

Release gate: run `npm ci` and `npm run verify` twice (source folder and Git working tree) before push.
## R2 build hotfix

- Fixes the TypeScript build gate found during the first Windows verification: Zod envelope rows now cross into current domain arrays through one explicit validated-transport boundary instead of 24 direct structural assertions.
- Fixes the backup integrity test fixture so `Message` includes current required `worldId` and `status` fields.
- No IndexedDB schema change and no Backup format bump: still IndexedDB V18 / Backup V12.
- The first R1 ZIP must not be deployed; use `ai-companion-phone-v0.5.0-alpha.5.1.12-windows-safe-r2.zip`.
## R3 build hotfix

- Fixes the remaining TypeScript build-gate error exposed by Windows verification: the backup integrity test fixture now uses the real `MessageStatus` value `delivered` instead of the invalid `sent`.
- R2 already fixed the 24 Backup transport/domain boundary assertions; R3 is the final one-line fixture correction required to let `vue-tsc` continue past `backupValidation.test.ts`.
- No runtime behavior, IndexedDB schema, or Backup format change: still IndexedDB V18 / Backup V12.
- Do not deploy R1 or R2; use `ai-companion-phone-v0.5.0-alpha.5.1.12-windows-safe-r3.zip`.

