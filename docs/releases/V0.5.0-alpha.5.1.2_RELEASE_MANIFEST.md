# V0.5.0-alpha.5.1.2 · Moments 构建修复

- 修复 alpha.5.1.1 `MomentsView.vue` 引用了 `showCreateMenu`，但 `<script setup>` 中遗漏 `ref` 声明，导致 `vue-tsc -b` 的 5 个 TS2304/TS2339 错误。
- 补回 `const showCreateMenu = ref(false)`，保留原有发布菜单显示与关闭行为；未修改朋友圈互动、Persona、封面功能。
- IndexedDB V18、Backup V12 不变；无需清数据。
- alpha.5.1.1 在用户 Windows 上已验证 34 个测试文件 / 298 个测试通过；该版本 Build 未通过。本修复仍须通过 Windows `npm run verify`（测试和构建）后才能部署。
