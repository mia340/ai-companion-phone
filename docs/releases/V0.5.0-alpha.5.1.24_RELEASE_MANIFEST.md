# V0.5.0-alpha.5.1.24 Release Manifest

- Version: `0.5.0-alpha.5.1.24`
- IndexedDB: V18
- Backup: V12
- Launcher: Grid V13
- HomeLayout revision: 12

## 本轮重点

1. 修复 Windows Safe ZIP 的文件名编码：把历史 CP437/mojibake 中文文档名还原为 UTF-8 中文名，并以 UTF-8 ZIP filename flag 重新打包，避免 PowerShell `Expand-Archive` 报“路径中具有非法字符”。
2. 保留 alpha.5.1.23 的 TypeScript 发布门禁热修：`HomeScreen.vue` 中无引用的 `pageDiagnosticLine()` 已删除。
3. 保留 alpha.5.1.22 的 canonical HomeLayout 修复：已有 Grid 布局时，以 `homeLayoutPages[]` 为成员事实源。
4. IndexedDB V18 / Backup V12 / Launcher Grid V13 / HomeLayout revision 12 不变。
5. 测试定义仍为 `42 files / 349 it-test declarations`；最终状态以 Windows 两次 `npm run verify` 为准。
