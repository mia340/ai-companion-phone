# V0.5.0-alpha.4.4 Release Manifest

## 目标

将角色卡内嵌用户 Persona 识别从“补某一张卡的格式”升级为社区兼容识别层。实现按字段语义、区块标签、世界书条目和社区扩展键识别，不依赖具体角色名或具体用户姓名。

## 核心变更

- creator_notes / 自由文本支持更多社区标签：用户设定、用户人设、用户档案、玩家设定、主控人设、我的设定、自机设定、user persona/profile/settings 等。
- 标签不再要求独占一行，可识别“作者说明…… [我的设定]姓名,年龄……”形式。
- 世界书 Persona 条目标签扩展到玩家/主控/主角/自机/我的设定等常见社区写法。
- 增加社区 JSON 深层字段识别：`user_persona`、`user_profile`、`player_profile` 等语义字段及其对象形式。
- description / personality / scenario / system_prompt / post_history_instructions 均可使用既有安全 `{{user}}` 独立 Persona 识别器。
- 继续排除 `user_personal_room`、住所、座驾、衣橱、头像、手机、NPC 等“用户相关但不是 Persona”的资源。
- “user 人设自拟 / 请填写用户设定”只有提示、没有具体身份信息时不会创建伪 Persona。
- 年龄解析支持 `25岁(生日...)`、`25岁（生日...）` 等社区常见写法。

## 社区样本审计

本轮使用用户提供的社区资源包做格式审计：

- 可读取 JSON：49 份；
- 发现明确 Persona / 用户背景标签的文件：13 份；
- 主要来源：世界书 user/用户人设条目、creator_notes 标注区块、社区扩展字段；
- 同时观察到 `user_personal_room` 与“user 人设自拟”两类高风险误判格式，均保留排除策略。

注意：角色卡若本身没有提供独立用户 Persona，只出现普通 `{{user}}` 剧情引用，则不会凭空猜姓名或创建身份。

## 数据兼容

- IndexedDB：V16（不变）
- Backup：V11（不变）
- 无数据库迁移

## 验证目标

- `npm run verify`
- 预期 Vitest：30 个测试文件，277 个测试（新增 2 个社区 Persona 泛化回归测试）
- `npm run build`
