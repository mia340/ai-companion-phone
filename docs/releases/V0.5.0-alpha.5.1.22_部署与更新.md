# V0.5.0-alpha.5.1.22 部署与更新

部署包：`D:\ai\ai-companion-phone-v0.5.0-alpha.5.1.22-windows-safe.zip`

固定流程：解压 → `npm ci` → `npm run verify` → 确认 Git 仓库干净 → robocopy 同步 → 第二次 `npm ci` / `npm run verify` → commit → push。

不要删除旧站点，不要清除浏览器站点数据、IndexedDB 或 LocalStorage。

## 专项验收

- 启动后检查历史第二页：若它只包含旧 Dock 重复 App，本版 revision 12 会自动清理并回写。
- 长按桌面 → 编辑 → 布局诊断：应能逐项看到 `type/key/x/y/w/h`、DOM、painted 与结构问题，并可复制诊断 JSON。
- 在视觉空白的非第一页执行“备份并回收当前视觉空白页”，应先保存 recovery snapshot 再回收页；业务数据不删除。
- Prompt Debug → WorldBook Activation Inspector：关键词条目应显示主/辅助关键词、scan depth、selective logic 等。
- Context Inspector：应显示 Character / Persona / Prompt Preset / Memory / WorldBook / Recent Messages 等来源和估算 token。
