# V0.5.0-alpha.5.5.1 Release Manifest

Release name: **Couple Board V1 Launcher Test Hotfix / 心跳飞行棋发布热修**

## Included

1. Synchronize `companionNavigation.test.ts` catalog expectations with the new `couple-board` Launcher app.
2. Mark current-layout Launcher fixtures with `HOME_LAYOUT_REVISION = 13`, so they are not interpreted as legacy revision 0 data.
3. Keep the real revision 12 -> 13 migration test unchanged: legacy users still receive `couple-board` exactly once.
4. No Couple Board Runtime, UI, persistence, IndexedDB, Backup, or Launcher migration behavior change.

## Data / schema

```text
App version:              0.5.0-alpha.5.5.1
IndexedDB:                V18 (unchanged)
Backup:                   V12 (unchanged)
Launcher Grid:            V13
HomeLayout revision:      13
Test file declarations:   66
Test declarations:        563
```

## Release gate

The package must pass, twice (extracted source and clean Git repo):

```text
npm ci
npm run verify
```

Then it must pass `release:git-working`, `release:staged-check`, `git diff --cached --check`, forbidden staged path checks, commit, and push.

The artifact-generation environment does not claim a completed Windows-equivalent two-pass verification. Windows/CI output is authoritative.
