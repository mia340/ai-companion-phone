# V0.5.0-alpha.5.1.16 Release Manifest

- Version: `0.5.0-alpha.5.1.16`
- IndexedDB: V18
- Backup: V12
- HomeLayout Revision: 9
- Test definitions: 41 files / 338 declarations (final gate uses Windows `npm run verify`)
- Ghost-page GC: stable empty pages are filtered at render time, and the active page gets an additional DOM-level empty-page repair.
- App Folder V1: drag one App onto another to create an iPhone-style folder; drag more Apps onto the folder to append members.
- Folder lifecycle: folder is a 1×1 Grid Item, can move with the Launcher, can be renamed, and auto-dissolves when only one App remains.
- HomeLayout schema / layout backup: supports `folder` items without an IndexedDB schema migration.

Release gate: run `npm ci` and `npm run verify` twice (source folder and Git working tree) before push.

- R2 hotfix: remove an unused internal helper (`folderContainsApp`) that caused `vue-tsc` TS6133 under `noUnusedLocals`; runtime behavior is unchanged.
