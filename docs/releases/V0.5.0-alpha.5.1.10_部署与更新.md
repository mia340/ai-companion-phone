# V0.5.0-alpha.5.1.10 部署与更新

按长期固定流程：**解压 → 第一次 verify → 检查 Git 干净 → robocopy → 第二次 verify → commit → push**。

源码包：`ai-companion-phone-v0.5.0-alpha.5.1.10-windows-safe.zip`

部署前后均不要清除浏览器站点数据；本版不升级 IndexedDB。

验收重点：

- 第一页左侧不再出现竖白线；
- 用手指/鼠标左右拖动即可翻页；
- 不存在“新增一页”入口；
- 只剩一页内容时分页圆点自动消失；
- 某一页最后一个 App 被移走/隐藏后，该空页自动消失；
- Dock、Widget、跨页拖拽仍正常。
