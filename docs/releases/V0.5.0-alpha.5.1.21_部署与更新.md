# V0.5.0-alpha.5.1.21 部署与更新

部署包：`D:\ai\ai-companion-phone-v0.5.0-alpha.5.1.21-windows-safe.zip`

固定流程：解压 → `npm ci` → `npm run verify` → 确认 Git 仓库干净 → robocopy 同步 → 第二次 `npm ci` / `npm run verify` → commit → push。

不要删除旧站点，不要清除浏览器站点数据、IndexedDB 或 LocalStorage。

## 专项验收

- 滑到长期残留的空白第二页，保持约 1 秒：系统应先尝试无损折叠；连续确认仍空白时，第二页和第二个圆点必须回收。
- 回收前会把该页 JSON 保存到本机 recovery slot，不删除业务数据。
- Prompt Debug → WorldBook Activation Inspector 应能看到 match/递归/token/priority 等细节。
- Context Inspector 应能看到主要 Context 来源的字符数与近似 token。
