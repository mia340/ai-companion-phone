# V0.5.0-alpha.5.4.0 部署与更新

本版本为 Relationship Arc V1。无 IndexedDB schema / Backup 迁移，不需要清除站点数据。

部署必须使用 Windows Safe ZIP，并依次完成：ZIP SHA-256 → source identity → `npm ci` → 第一轮 `npm run verify` → Git preflight → `/MIR` 同步 → 第二轮 `npm ci && npm run verify` → staged safety → commit → push。

预期测试矩阵：

```text
Test Files  65 passed (65)
Tests       547 passed (547)
```

测试通过后还必须继续看到 `vue-tsc -b && vite build` 成功，以及 Release Safety 全部 `[release-safety] OK`。

专项人工验收：进入「时光 → 关系脉络」；未选择角色时可见角色脉络概览；选择角色后按真实时间显示阶段与节点；只有 relationship state evidence 显示 before → after；AI 候选需手动采用；带回聊天只写草稿且包含 arc/node/evidence provenance；隐藏 evidence 后脉络与已确认摘要同步失效/重算。
