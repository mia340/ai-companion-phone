# V0.5.0-alpha.5.9.1 部署与更新

把 ZIP 与 `deploy-v0.5.0-alpha.5.9.1-ps51-safe.ps1` 放到 `D:\ai`，不要手工覆盖 Git 仓库。

```powershell
cd D:\ai
$zip = "D:\ai\ai-companion-phone-v0.5.0-alpha.5.9.1-windows-safe.zip"
$script = "D:\ai\deploy-v0.5.0-alpha.5.9.1-ps51-safe.ps1"
Get-FileHash $zip -Algorithm SHA256
Get-FileHash $script -Algorithm SHA256
powershell -NoProfile -ExecutionPolicy Bypass -File $script
```

本版不升级 IndexedDB / Backup / Wardrobe schema。已有 5.9.0 穿搭数据直接兼容。
