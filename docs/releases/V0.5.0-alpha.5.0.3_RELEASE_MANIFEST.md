# V0.5.0-alpha.5.0.3 Release Manifest

Persona extraction hotfix on top of Social Runtime V1.

- 修复 description 内 `{{user}}固定对应某姓名` / `{{user}}对应某姓名` / `{{user}}就是某姓名` 这类明确映射声明没有进入 Persona 候选的问题。
- 映射声明只有在同一块文本具有年龄、职业、身份等 Persona 内容信号时才接受，避免把普通剧情中的 `{{user}}就是朋友` 之类句子误识别为 Persona。
- 映射声明后续的结构化字段（如 `年龄:`、`职业:`）会一起收集，而不是只截取第一行。
- 姓名仍然来自当前角色卡内容；不存在任何全局或预设“通用 Persona 姓名”。
- 保留姓名外层包裹符归一化：`{姓名`、`【姓名】`、`「Name」` 等。
- Social Runtime V1、角色回复用户评论、角色之间评论互动、持久队列保持不变。
- IndexedDB V17、Backup V11 保持不变。
- 测试定义保持 32 个测试文件 / 290 个测试。
