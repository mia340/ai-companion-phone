# V0.5.0-alpha.5.4.1 部署与更新

本版本为 Relationship Arc V1 发布热修。5.4.0 第一次 `npm run verify` 在复制到 Git 仓库之前停止，因此可以直接部署 5.4.1；不需要清理 Git 仓库，也不需要部署 5.4.0。

修复点：角色事件在投影前收窄，防止跨角色 evidence；完整 evidence projection 保留稳定 `evidenceKey`，保证已确认摘要命中，evidence 变化后仍会失效。

无 IndexedDB schema / Backup 迁移，不需要清除站点数据。

预期测试矩阵：

```text
Test Files  65 passed (65)
Tests       547 passed (547)
```

测试通过后还必须继续看到 `vue-tsc -b && vite build` 成功，以及 Release Safety 全部 `[release-safety] OK`。
