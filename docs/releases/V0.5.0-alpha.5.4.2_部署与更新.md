# V0.5.0-alpha.5.4.2 部署与更新

本版本为 Relationship Arc V1 TypeScript 发布热修。5.4.1 第一轮 Vitest 已通过到编译阶段，随后 `vue-tsc -b` 因 `SharedTimelineView.vue` 的可选 event 访问报 TS18048；脚本在复制 Git 仓库之前停止，因此可以直接部署 5.4.2。

修复点：`contextSenderLabel()` 将 `event.characterName` 改为安全的 `event?.characterName`。无运行时功能改变，无 IndexedDB / Backup 迁移。

预期测试矩阵：

```text
Test Files  65 passed (65)
Tests       547 passed (547)
```

测试通过后还必须继续看到 `vue-tsc -b && vite build` 成功，以及 Release Safety 全部 `[release-safety] OK`。
