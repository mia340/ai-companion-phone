# V0.5.0-alpha.5.1.15 Release Manifest

- Version: `0.5.0-alpha.5.1.15`
- IndexedDB: V18
- Backup: V12
- HomeLayout Revision: 8
- Test definitions: 41 files / 336 declarations (final gate uses Windows `npm run verify`)
- Ghost-page hardening: stale drag preview cannot render outside an active drag.
- Pointer lifecycle: drag cleanup is guaranteed by `try/finally`.
- Startup self-heal: normalized pages are written back when old layout/revision differs.
- Render fallback: a stable page with no visible Launcher item is removed and canonical App/Widget lists are recomputed.

Release gate: run `npm ci` and `npm run verify` twice (source folder and Git working tree) before push.
