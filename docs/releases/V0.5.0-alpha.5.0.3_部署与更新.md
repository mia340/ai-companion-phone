# V0.5.0-alpha.5.0.3 部署与更新

1. 解压到新的目录，不覆盖旧版本源码目录。
2. 执行 `npm install`。
3. 执行 `npm run verify`。
4. 预期测试：32 个测试文件 / 290 个测试全部通过，随后 Vite build 成功。
5. 本版本数据库仍为 IndexedDB V17，Backup V11，不需要清空现有数据。

本次仅修复 Persona 映射声明提取，不改变 Social Runtime V1 的数据结构或行为。
