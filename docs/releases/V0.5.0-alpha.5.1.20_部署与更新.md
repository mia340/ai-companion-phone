# V0.5.0-alpha.5.1.20 部署与更新

部署包：`D:\ai\ai-companion-phone-v0.5.0-alpha.5.1.20-windows-safe.zip`

固定流程：解压 → `npm ci` → `npm run verify` → 确认 Git 仓库干净 → robocopy 同步 → 第二次 `npm ci` / `npm run verify` → commit → push。

不要删除旧站点，不要清除浏览器站点数据、IndexedDB 或 LocalStorage。

## 专项验收

- 点击“一起听”Widget 主体应进入音乐 App。
- Widget 右侧播放键：存在可播放 audioUrl 时直接播放/暂停；无音源时进入音乐 App。
- 滑到历史幽灵第二页后，若页面没有真实绘制项目，应自动安全合并回前页并删除第二个分页圆点。
