# V0.5.0-alpha.5.1.2 Windows 更新

1. 下载完整源码 ZIP 到 `D:\ai` 并解压为独立的 `D:\ai\ai-companion-phone-v0.5.0-alpha.5.1.2`。
2. 运行 `npm install`，再执行 `npm run verify`。确认 34 个测试文件、298 个测试全部通过，且 `vue-tsc -b && vite build` 成功。
3. `npm run dev` 实测朋友圈发布菜单、封面上传/恢复、独立设置/新互动页面。
4. 所有验证完成后再同步到 Git 仓库并推送。不得把尚未通过 Build 的 alpha.5.1.1 部署上线。

数据库和备份版本不变：IndexedDB V18、Backup V12；不要清除站点数据。
