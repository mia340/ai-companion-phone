# V0.5.0-alpha.5.5.2 Release Manifest

Release name: **Couple Board V1 Launcher Fixture Hotfix / 心跳飞行棋 Launcher 测试夹具热修**

## Included

1. Remove the stale `homeLayoutRevision: 7` duplicate key from the ghost-page normalization fixture in `companionNavigation.test.ts`.
2. Keep the fixture on `HOME_LAYOUT_REVISION = 13`, so this test exercises page normalization only and does not trigger the real Couple Board migration.
3. Eliminate the Vite/esbuild duplicate-object-key warning seen in the 5.5.1 Windows verify.
4. Keep the real revision 12 -> 13 `couple-board` migration test unchanged in `homeLayoutBackup.test.ts`.
5. No production Runtime, Couple Board UI/rules, persistence, IndexedDB, Backup, or Launcher migration behavior change.

## Data / schema

```text
App version:              0.5.0-alpha.5.5.2
IndexedDB:                V18 (unchanged)
Backup:                   V12 (unchanged)
Launcher Grid:            V13
HomeLayout revision:      13
Test file declarations:   66
Test declarations:        563
```

## Observed 5.5.1 gate

```text
Test Files  1 failed | 65 passed (66)
Tests       1 failed | 562 passed (563)
```

The remaining failure was the duplicate-revision test fixture, not Couple Board production code.

## Release gate

The package must pass, twice (extracted source and clean Git repo):

```text
npm ci
npm run verify
```

Then it must pass `release:git-working`, `release:staged-check`, `git diff --cached --check`, forbidden staged path checks, commit, and push.

Windows/CI output is authoritative.
