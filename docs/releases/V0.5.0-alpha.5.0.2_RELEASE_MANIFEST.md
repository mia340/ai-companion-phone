# V0.5.0-alpha.5.0.2 Release Manifest

Syntax hotfix on top of Social Runtime V1.

- 修复 `normalizePersonaNameToken` 中 Unicode (`/u`) 正则的非法 `\"` identity escape。
- 双引号在 RegExp 字符类中无需转义，统一改为 `"` 字面字符。
- 保留社区 Persona 姓名外层包裹符归一化：`{洛梨`、`【北柠】`、`「Alex」` 等。
- Social Runtime V1、角色回复用户评论、角色之间评论互动、持久队列均保持不变。
- IndexedDB V17、Backup V11 保持不变。
- 测试定义保持 32 个测试文件 / 290 个测试。
