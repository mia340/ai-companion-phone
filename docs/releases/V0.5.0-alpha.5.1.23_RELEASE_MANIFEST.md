# V0.5.0-alpha.5.1.23 Release Manifest

- Version: `0.5.0-alpha.5.1.23`
- IndexedDB: V18
- Backup: V12
- Launcher: Grid V13
- HomeLayout revision: 12

## 本轮重点

1. 删除 `HomeScreen.vue` 中无引用的 `pageDiagnosticLine()`，修复 Windows `vue-tsc` TS6133 发布门禁失败。
2. 保留 alpha.5.1.22 的 canonical HomeLayout 修复：已有 Grid 布局时，以 `homeLayoutPages[]` 为成员事实源，不从 legacy keys 复活幽灵项目。
3. 不改变数据库版本、备份版本、Launcher revision 或用户业务数据。
4. 静态测试定义仍为 `42 files / 349 it-test declarations`；最终状态以 Windows 两次 `npm run verify` 为准。
