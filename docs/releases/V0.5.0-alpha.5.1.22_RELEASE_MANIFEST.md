# V0.5.0-alpha.5.1.22 Release Manifest

- Version: `0.5.0-alpha.5.1.22`
- IndexedDB: V18
- Backup: V12
- Launcher: Grid V13
- HomeLayout revision: 12

## 本轮重点

1. HomeLayout Inspector V4：逐 Item 显示 persisted layout、DOM 存在性、当前页 painted 状态、坐标/尺寸与结构问题；支持复制诊断 JSON。
2. HomeLayout 结构修复：真实手机语义下，同一 App 不能同时存在于 Dock 与桌面/文件夹；读取旧布局时自动清理 Dock/Home 重复项、重复 App/Widget 与空页。
3. 幽灵页可恢复处理：当前视觉空白页可先写 recovery snapshot，再从 Launcher 回收；不删除角色、聊天、记忆等业务数据。
4. WorldBook Activation Inspector V4：新增 primary/secondary matches、selective logic、scan depth、regex/case/whole-word 诊断。
5. Context Inspector V3：Prompt section 保存估算 token，新增 Prompt Preset 来源与总 token 估算，可对照 API 实际 prompt token。
6. 静态测试定义：`42 files / 349 it-test declarations`；最终状态以 Windows 两次 `npm run verify` 为准。
