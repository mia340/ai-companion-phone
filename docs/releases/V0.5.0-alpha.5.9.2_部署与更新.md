# V0.5.0-alpha.5.9.2 部署与更新

使用发布目录中的 Windows Safe ZIP 与 PS5.1-safe 脚本部署。

本版不升级 IndexedDB 或 Backup schema。旧的 V1 / V1.1 穿搭数据会在运行时兼容迁移；角色新增的每日换装、分层配饰、自定义 HEX 颜色仍保存在现有 `appCustomizations.wardrobeState` 非索引字段中。

最终门禁：

```text
npm test
vue-tsc -b
vite build
release-safety
```
