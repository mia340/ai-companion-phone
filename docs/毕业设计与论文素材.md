# 毕业设计与论文素材记录

> 用途：持续记录需求来源、设计决策、技术难点、错误定位、解决方案、验证结果和可用于毕业论文的内容。本文件不替代开发日志；开发日志记录“做了什么”，本文件记录“为什么这样做、解决了什么问题、如何验证”。

---

## 1. 毕业设计题目候选

### 软件系统设计方向

《基于 Vue 与大语言模型的虚拟手机式 AI 陪伴系统设计与实现》

### 个性化角色方向

《面向个性化数字角色的 AI 陪伴交互系统设计与实现》

### 记忆与持续关系方向

《融合角色设定与多层记忆的 AI 陪伴系统设计与实现》

说明：

当前项目尚未接入真实大语言模型和四层记忆。最终题目应根据毕业设计完成度决定，避免题目覆盖尚未实现的内容。

---

## 2. 研究背景素材

现有聊天机器人多以通用问答为主，角色身份、情绪、关系和长期经历缺乏持续性。用户在不同会话中可能需要重复说明人物设定，不同角色也容易出现回复口吻趋同、上下文失效和信息串联等问题。

本项目以“虚拟手机”为统一交互载体，将通讯录、私聊、朋友圈、日记、记忆、世界状态和小游戏等功能组织成多个虚拟应用，使 AI 角色不只存在于单一聊天框中，而是形成可持续运行的数字生活空间。

---

## 3. 用户需求与核心问题

### 3.1 角色创建门槛

部分用户能够完整编写人物卡，部分用户只能描述零散想法，还有部分用户暂时没有明确人设。

系统需要同时支持：

- 逐项填写
- 整段人物卡导入
- 模板辅助选择

### 3.2 角色隔离

每个角色需要拥有独立的：

- 身份
- 人设
- 说话方式
- 会话
- 消息
- 情绪
- 关系
- 记忆
- 世界认知

### 3.3 角色一致性

面对相同问题，不同角色应表现出可辨识的语言差异；同一角色在多轮对话中应尽量维持稳定人设。

### 3.4 本地数据安全

角色资料、照片和消息需要持久化保存，并支持后续导出、备份和跨设备迁移。

### 3.5 模型可替换性

系统不能把界面直接绑定某个 AI 厂商，需要通过统一 Provider 接口接入不同模型。

---

## 4. 总体设计思路

### 4.1 虚拟手机式交互

PhoneFrame 提供统一手机外壳，StatusBar、DockBar 和 HomeScreen 构成 Phone OS。聊天、通讯录和资料页作为独立应用运行。

### 4.2 数据驱动

页面不再通过角色姓名写死跳转，而是使用 UUID、Character ID 和 Conversation ID 建立数据关联。

### 4.3 本地优先

原型阶段使用 Dexie 和 IndexedDB，先验证功能闭环，再增加后端与云同步。

### 4.4 Provider 抽象

页面通过统一 ModelProvider 调用 AI。未来增加真实模型时，保留聊天界面和数据结构。

---

## 5. 问题与解决记录

### 5.1 上传头像后显示 Base64 长字符串

**现象：**

通讯录头像位置出现：

`data:image/jpeg;base64,...`

**原因：**

照片被保存为 Data URL，但页面使用 `{{ character.avatar }}` 当作普通文字渲染。

**解决方案：**

新增 `CharacterAvatar.vue`：

- 图片头像使用 `<img>`
- Emoji 头像使用文本
- 图片统一 `object-fit: cover`

**涉及文件：**

- `src/components/CharacterAvatar.vue`
- `src/views/ContactsView.vue`
- `src/views/ChatList.vue`
- `src/views/ChatRoom.vue`

**验证结果：**

照片头像在通讯录和聊天中正确显示。

**论文可用章节：**

关键功能设计与实现——多类型头像资源的统一渲染。

---

### 5.2 点击苏晚却进入顾言聊天

**现象：**

用户在通讯录点击苏晚，聊天标题和内容却属于顾言。

**原因：**

早期演示代码写死：

- 林夏 → `conv-lin`
- 其他角色 → `conv-gu`

**解决方案：**

按以下条件查找会话：

```text
type === 'single'
memberIds.length === 1
memberIds[0] === character.id
```

不存在时自动创建单聊。

**设计意义：**

系统由固定演示数据转为真正的数据驱动。

**论文可用章节：**

数据库设计、角色与会话关联模型。

---

### 5.3 林夏在多个分组重复显示

**现象：**

林夏同时显示在“特别关心”和“朋友”。

**原因：**

一个角色允许属于多个分组，页面逐组直接渲染。

**解决方案：**

使用 `Set<CharacterId>` 记录已展示角色，按照分组顺序只显示一次。

**论文可用章节：**

通讯录分组与展示策略。

---

### 5.4 新增 Vue 组件后页面白屏

**现象：**

整个页面为空。

**真实原因：**

新建 `.vue` 文件为空或未保存，缺少 `<script>` 与 `<template>`。

**典型报错：**

`At least one <template> or <script> is required`

**解决方法：**

1. 查看 Vite 终端第一条红色错误；
2. 确认文件路径；
3. 完整保存组件；
4. 运行 `npm run build` 验证。

**工程启示：**

白屏不代表数据库损坏，应优先检查编译错误。

**论文可用章节：**

系统调试与工程实践。

---

### 5.5 TypeScript TS5096

**现象：**

构建时报错：

`allowImportingTsExtensions can only be used when noEmit or emitDeclarationOnly is set`

**原因：**

启用了 `allowImportingTsExtensions`，但未设置 `noEmit`。

**解决：**

在：

- `tsconfig.app.json`
- `tsconfig.node.json`

加入：

```json
"noEmit": true
```

**论文可用章节：**

开发环境与工程配置。

---

### 5.6 用户资料服务文件名大小写问题

**现象：**

页面白屏或模块无法导入。

**原因：**

文件名出现：

- `userprofile.ts`
- `userProfile.ts`

大小写不一致。

**解决：**

统一为：

`src/services/userProfile.ts`

并同步更新导入。

**工程启示：**

Windows 对大小写不敏感，但构建工具、Git 和部署环境可能敏感，项目必须统一命名规范。

---

### 5.7 同一 localhost 中角色数量不同

**现象：**

外部 Edge 中有苏晚，VS Code 内置浏览器中没有苏晚。

**初步误判：**

角色数据被删除。

**真实原因：**

两个浏览器环境拥有独立的 IndexedDB 和 localStorage。

**最终解决：**

- 固定外部 Edge 为验收环境
- 删除临时数据恢复逻辑
- 规划数据导出、导入和云同步

**系统设计启示：**

本地优先方案必须明确数据作用域，并提供用户可控的备份机制。

**论文可用章节：**

本地数据持久化、系统测试与局限性分析。

---

### 5.8 不同角色回复完全相同

**现象：**

林夏、顾言和自建角色都回复固定 Mock 文案。

**原因：**

Provider 只读取用户最后一句，没有接收角色资料。

**解决：**

扩展 ChatRequest，加入：

- 角色身份
- 人设
- 说话方式
- 背景
- 关系
- 心情
- 活动
- 喜好
- 用户资料
- 最近聊天

本地 MockProvider 根据关键词和角色风格选择不同模板。

**验证：**

同样发送：

- “你还记得自己的人设吗？”
- “你在干嘛？”
- “我今天有点难过。”

林夏与顾言表现出明显不同语气。

**当前边界：**

规则系统仅用于验证上下文架构，不能替代真实大语言模型。

**论文可用章节：**

AI 上下文构建与角色一致性机制。

---

## 6. 候选创新点

> 以下内容只能作为候选，需在最终功能和实验结果支持下再写入论文结论。

1. 虚拟手机式多应用 AI 陪伴交互框架。
2. 面向不同表达能力用户的三路径角色创建方法。
3. 角色、会话、消息和世界解耦的数据模型。
4. 可替换 AI Provider 与角色上下文构建机制。
5. 拟实现即时、短期、长期和核心四层记忆。
6. 面向角色长期一致性的关系、情绪与记忆协同机制。

---

## 7. 后续实验方案

### 7.1 角色创建方式对比

对比：

- 手动填写
- 模板辅助
- 人物卡导入

指标：

- 创建时间
- 完整字段数
- 修改次数
- 主观满意度

### 7.2 角色一致性评价

向多个角色发送同一组问题，评价：

- 人设符合度
- 语言可区分度
- 多轮稳定性
- 角色信息泄漏率
- 用户沉浸感

### 7.3 可用性测试

任务：

- 创建角色
- 上传头像
- 打开聊天
- 修改用户资料
- 查找历史消息

指标：

- 任务完成率
- 完成时间
- 操作错误数
- SUS 量表
- 满意度

### 7.4 性能测试

- 初次加载时间
- IndexedDB 写入延迟
- 消息查询时间
- 1000 条消息下滚动性能
- Base64 头像存储空间
- 数据库升级成功率

### 7.5 Provider 对比

对比：

- 本地规则 Mock
- 通用大模型
- 注入角色上下文的大模型
- 加入记忆检索的大模型

评价：

- 角色一致性
- 回复自然度
- 上下文相关性
- 响应时间
- 调用成本

---

## 8. 毕业论文建议目录

### 第一章 绪论

- 研究背景
- 研究意义
- 国内外相关研究
- 主要研究内容
- 论文结构

### 第二章 需求分析与关键技术

- 用户需求
- 功能需求
- 非功能需求
- Vue 与 PWA
- IndexedDB
- 大语言模型与提示词
- 角色一致性问题

### 第三章 系统总体设计

- 系统架构
- 功能模块
- 数据模型
- 路由设计
- Provider 架构
- 数据安全设计

### 第四章 关键功能设计与实现

- 虚拟手机界面
- 角色创建
- 图片头像
- 通讯录
- 单聊
- 用户资料
- 角色化回复

### 第五章 记忆、关系与角色一致性

在相关功能完成后撰写：

- 四层记忆
- 记忆检索
- 关系数值
- 情绪更新
- 上下文构建

### 第六章 系统测试与分析

- 功能测试
- 可用性测试
- 性能测试
- 角色一致性测试
- 结果讨论

### 第七章 总结与展望

- 主要成果
- 当前不足
- 云同步
- 群聊
- 主动消息
- 世界演化

---

## 9. 开发证据保存清单

后续持续保存：

- 每个版本功能截图
- 修改前后对比图
- 报错截图
- 构建成功日志
- 测试输入与输出
- 关键代码版本
- 设计草图
- 用户反馈
- Git 提交记录

重点保存：

1. Base64 头像错误与修复对比
2. 点击苏晚进入顾言的错误与修复
3. 林夏与顾言角色化回复差异
4. 用户资料与聊天头像同步
5. 外部 Edge 与 VS Code 内置浏览器数据差异
6. `npm run build` 成功截图

---

## 10. 后续问题记录模板

### 问题名称

**现象：**

**出现条件：**

**初步判断：**

**真实原因：**

**解决方案：**

**涉及文件：**

**验证方法：**

**结果：**

**对系统设计的启示：**

**可用于论文的章节：**
