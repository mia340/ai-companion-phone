# V0.5.0-alpha.4.3.1 Release Manifest

## Identity

- App: AI Companion Phone
- Version: `0.5.0-alpha.4.3.1`
- IndexedDB: V16
- Backup: V11
- Theme: CI Persona Age Parsing Hotfix

## Scope

- Fix creator_notes embedded Persona age parsing for values such as `23岁(生日12月3日)` and `23岁（生日…）`.
- Keep alpha.4.3 threaded Moments replies and multi-character activity unchanged.
- Add `npm run verify` (`npm test && npm run build`) so local release verification stops on test failure.

## Data compatibility

- IndexedDB remains **V16**.
- Backup remains **V11**.
- No migration and no data reset required.

## Required Windows acceptance

```powershell
npm install
npm run verify
npm run dev
```

Expected verification target: **30 test files / 275 tests**, then a successful Vite production build.
