# V0.5.0-alpha.4.2 Release Manifest

## Identity

- App: AI Companion Phone
- Version: `0.5.0-alpha.4.2`
- IndexedDB: V16
- Backup: V11
- Theme: Community Chat Compatibility

## Scope

- Community-first chat presentation priority.
- Opening / greeting UI contract aligned with later AI replies.
- WorldBook HTML greeting repair without an extra model call.
- Existing greeting seed migration-on-load.
- `<br>` / `&lt;br&gt;` compatibility fix.
- Responsive author HTML and no double bubble for author-owned surfaces.
- Chat settings wording aligned with author-first / user-override semantics.

## Main changed code

- `src/views/ChatRoom.vue`
- `src/services/communityUiRuntime.ts`
- `src/services/communityUiRuntime.test.ts`
- `src/components/chat/SafeRichHtml.vue`
- `src/components/chat/ChatMessageItem.vue`
- `src/components/chat/ChatSettingsPanel.vue`

## Test definition

- Test files: 30
- Test cases: 272
- New alpha.4.2 tests: 2

## Validation performed in packaging environment

- TypeScript transpile syntax check for changed `.ts` and Vue `<script setup>`: PASS.
- Exact uploaded Su Yuchen JSON Community UI compiler sanity: PASS (`html-contract`, repaired, no escaped `<br>`).
- Full Vitest: NOT RUN in container; local `node_modules` is incomplete and `vitest` is absent.
- Full `vue-tsc -b && vite build`: NOT CLAIMED in container; Windows regression is required before Git commit.

## Expected Windows acceptance

```text
Test Files  30 passed (30)
Tests       272 passed (272)
```

Then `npm run build` must complete successfully.
