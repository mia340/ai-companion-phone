# 新对话请先阅读 · V0.4.3.5

请先完整阅读 `AI续开发交接说明_V0.4.3.5.md`，再检查 V0.4.3.5 项目 ZIP。

当前基线：应用 V0.4.3.5 / IndexedDB V10 / backup V9。

本版重点修复：

- 新角色不再统一显示“刚刚来到这个世界 / 期待认识你”；有卡内活动则识别，没有就留空。
- 历史角色打开通讯录/聊天时自动清理旧占位活动，并可从开场明确状态恢复关系。
- `{{user}}` 只作为占位符；当前聊天 Persona 才是运行时用户身份。
- 角色卡内嵌世界书中的“user人设 / 用户人设 / user persona”现在可识别为角色专属 Persona。
- 有明确 user 人设就可自动建立专属 Persona；只有剧情占位而无安全姓名时不猜名、不污染全局“我的资料”。
- 普通 `<br>` 开场转换为真实换行；alternate greetings 也统一经过宏、Regex、Rich UI 和状态提取。

真实回归：墨清尘卡可识别“洛梨 / 20岁 / 师徒 / 负手立于田埂”；“离婚的诱惑”保留其 `{{user}}` 剧情身份但不会擅自猜用户姓名。

下一主线建议：V0.4.3.6 WorldBook Engine V2 + Preset Prompt Manager + Regex 编辑器 + Theme Runtime V1，并把 User Resolver 来源显示加入 Prompt Debug。

产品原则：尽可能兼容 Tavo / SillyTavern 社区资源，但第三方 JavaScript 继续只归档、不直接执行；角色世界中的用户身份不能擅自覆盖全局真实用户资料。
