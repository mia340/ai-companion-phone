# V0.5.0-alpha.5.0.2 部署与更新

这是 alpha.5.0.1 的语法热修复。

## 修复

alpha.5.0.1 在 `normalizePersonaNameToken` 中使用了带 `/u` 的正则：

```ts
/^[\s\"'“”‘’`]+/u
```

其中 `\"` 在 Unicode RegExp 模式下属于非法 identity escape，导致 Vitest/Rollup 在收集测试文件时直接 SyntaxError。现在改为：

```ts
/^[\s"'“”‘’`]+/u
```

结尾字符类同样修正。

## 数据兼容

- IndexedDB: V17
- Backup: V11
- 不需要清除角色、聊天、Persona、朋友圈或 Social Runtime 数据。

## Windows 验证

```powershell
npm install
npm run verify
```

预期测试定义：32 个测试文件，290 个测试。
